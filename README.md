# [Project Name]

> [One-sentence description of what this project does.]

<!-- Optional: badges go here once CI is configured
![CI](https://github.com/[org]/[repo]/actions/workflows/ci.yml/badge.svg)
-->

## Quick Start

### Prerequisites

- [List required tools and versions, e.g. Node 20+, Python 3.12+]
- [Any required accounts or access, e.g. AWS credentials]
- [Claude Code](https://claude.ai/code) with the superpowers plugin enabled (`superpowers@claude-plugins-official`)

### Install

```bash
# Clone
git clone https://github.com/[org]/[repo].git
cd [repo]

# Install dependencies
[install command, e.g. npm install / pip install -e ".[dev]"]
```

### First run

```bash
[command to run the project or its tests to verify the setup works]
```

## Usage

[One or two paragraphs describing what the project does from a user or operator perspective. What problems does it solve? Who uses it and how?]

## Documentation

All project documentation lives in [`docs/`](docs/) — an Obsidian vault containing architecture decisions, feature specs, concepts, guides, and user-facing docs.

Open `docs/` in Obsidian for the full experience (graph view, backlinks, Bases queries). The docs are plain markdown and readable without Obsidian.

**Start here:** [`docs/meta/claude-context.md`](docs/meta/claude-context.md)

## Development Workflow

This project uses a documentation-driven development workflow. All project knowledge lives in the `docs/` Obsidian vault.

**First-time setup:** Open `docs/` as an Obsidian vault, then run `/docs-init` in Claude Code.

**Ongoing work:** See [docs/guides/workflow.md](docs/guides/workflow.md) for the full stage-by-stage process — from idea capture through spec, implementation, and close.

Every session starts with `/docs-context`.

## Contributing

[Contribution guidelines — how to propose changes, branch naming, PR process, etc.]

## License

[License name] — see [LICENSE](LICENSE) for details.
